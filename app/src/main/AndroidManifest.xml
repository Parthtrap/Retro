<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.parthtrap.donationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DonationApp">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/API_KEY" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".SignUpMapInfoPage"></activity>
        <activity android:name=".ExchangeItemViewPage" />
        <activity android:name=".ExchangeItemUploadPage" />
        <activity android:name=".LogInPage" />
        <activity android:name=".ProfileDisplay" />
        <activity android:name=".HomePage" />
        <activity android:name=".SignUpInfoInputPage" />
        <activity android:name=".SignUpPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>